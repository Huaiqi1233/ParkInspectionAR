# 现场AR问题标注 — 项目需求说明书

> 本文档为「现场AR问题标注」题目的完整需求理解，供后续开发/评审 agent 参考。
> 来源：题目原图（需求文档）+ 当前工作区交接状态（`.dshwolf/STATUS.md`）。
> 最后更新：2026-08-29

---

## 1. 项目背景与目标

园区巡检人员在发现**设备破损、设备异常或安全隐患**时，希望用手机在**现场放置一个 AR 标记**并上报问题；管理人员能在**电脑网页**中查看这些问题并更新处理状态。

目标：完成一个**小型原型**，以基础功能为主，**不要求复杂架构和精美界面**，打通完整闭环。

### 核心闭环

```
Unity AR 拍摄标记上报  →  Go 后端保存  →  React 界面查看问题  →  React 修改处理状态
```

---

## 2. 固定技术栈

| 部分 | 技术要求 | 作用 |
| :--- | :--- | :--- |
| 移动端 | Unity LTS、C#、AR Foundation | 检测水平平面、放置标记、上报问题 |
| 管理端 | React、TypeScript | 查看问题、修改状态 |
| 后端 | Go | 提供 API 并持久化数据 |

补充约束：
- Unity 可选择 **Android + ARCore** 或 **iOS + ARKit**；
- Go 框架不限；
- 数据存储持有 **SQLite 或本地 JSON 文件**；
- 可使用 AI 辅助开发，但**不得嵌入大模型**做产品化大功能。

---

## 3. 必做功能

### 3.1 Unity AR 移动端
1. 在**支持 AR 的真机**上运行；
2. 检测真实环境中的**水平平面**；
3. 点击检测到的平面，在**命中位置**放置一个简单标记；
4. 提交问题字段：
   - 标题：**必填**；
   - 描述：可选；
   - 优先级：`low` / `medium` / `high`；
   - 位置：标记的 `x/y/z` 坐标；
5. 提交**成功或失败**时，给用户明确提示；
6. 提交失败时，不需要在 AR 场景中保留标记。

> 标记可直接使用立方体、球体等 Unity 基础对象，不考美术效果。

### 3.2 React 管理端
1. 从 Go 后端拉取问题列表；
2. 至少显示 **标题、优先级、状态**；
3. 能将状态修改为：`open` / `in_progress` / `resolved`；
4. 后端不可用时，页面应显示**错误提示**，不能直接白屏。

### 3.3 Go 后端
1. 提供简单的服务运行状态检查（healthz）；
2. 接收并保存 Unity 上报的问题；
3. 返回已存储的问题列表；
4. 根据问题 ID 修改处理状态；
5. 为新问题生成**唯一 ID**，默认状态为 `open`；
6. 标题为空或字段错误时，返回**明确的参数错误**；
7. 数据在 Go 服务**重启后仍然存在**；
8. Unity 和 React 使用**同一个** Go 后端和同一份数据；
9. React 在浏览器中能正常访问后端（CORS）。

> 接口路径和响应格式可自行设计，需在 README 中简要说明。

---

## 4. 基础数据结构

三端字段名称应保持一致：

```json
{
  "id": "自动生成",
  "title": "入口路面破损",
  "description": "注意路面存在裂缝",
  "priority": "high",
  "status": "open",
  "position": { "x": 0.42, "y": 0.89, "z": 1.26 },
  "createdAt": "自动生成",
  "updatedAt": "自动生成"
}
```

> 不要求应用重启后把历史标记恢复到完全相同的真实物理位置。

---

## 5. AI 使用说明（AI_LOG.md）

在项目根目录提交 **AI_LOG.md**，按实际顺序保存所有**主动发给 AI 的问题或指令**（含追问和纠偏）。每条记录格式：

```
## P-001

- 目标：解决什么问题

### Prompt 原文

> 此处直接贴发给 AI 的问题或指令

### 结果

- 采用 / 修改后采用 / 未采用
- 需要说明如何验证
```

要求：
- 必须保留 **Prompt 原文**，不能只写总结；
- 不记录 IDE 的逐字自动补全；
- 至少记录一次**检查、修改或放弃 AI 结果**的过程；
- 能够解释最终提交的核心代码；
- **不得提交** API Key、Token、账号、隐私等敏感信息（需用 `[REDACTED: ...]` 占位）。

---

## 6. 交付内容

1. Unity、React、Go 的**完整源码**；
2. 简短 **README.md**：版本 / 三端启动方式 / Unity 真机如何配置 Go 后端地址 / 已完成、未完成和已知问题；
3. **AI_LOG.md**；
4. 两段 **3-5 分钟演示视频**（手机 + 电脑），至少展示：
   - 真机识别平面并放置标记；
   - 手机提交问题；
   - React 刷新后看到该问题；
   - React 修改状态；
   - Go 重启后数据仍然存在。

> 不强制提交 APK、不强制部署公网。

### 交付方式
- GitHub / Gitee 仓库（需完整 Git 历史）、百度网盘或其他无权限限制的可下载方式；
- 提供仓库/文件访问链接；如设权限/提取码/有效期需说明并保证评审期间可访问。

---

## 7. 加分项（参考，不要求全部实现）

- 登录、权限或人员管理；
- React 筛选、详情或编辑；
- Unity 应用构建及编译；
- 按优先级或状态改变 AR 标记样式；
- 实时数据或 WebSocket；
- 跨设备、跨会话定位的合理方案；
- Docker、自动化测试或更规范工程组织；
- 更完整易用的 UI 和交互；
- 结合业务场景设计有实际价值的 AI 功能。

> 评审不以功能数量区分高下，重点看业务价值、实现质量和稳定性。

---

## 8. 重点关注（评分维度）

1. 是否完成**真实 AR**（非普通 3D 场景或屏幕固定物体）；
2. Unity、React、Go 是否形成**可运行小闭环**；
3. 遇到问题时能否**定位、修正并验证**；
4. 是否**合理使用 AI**（非直接复制无解释代码）；
5. 是否**如实说明未完成内容和已知问题**。

---

## 9. 当前实现状态（交接快照）

> 三端闭环已全部完成并经真机验证，详见 `README.md` 与 `docs/blackbox-test-report.md`。

- ✅ **Phase 1 Go 后端**（`server/`）：6 端点 + healthz + SQLite 持久化 + 防重放，验收全过
- ✅ **Phase 3 React 管理端**（`web/`）：列表/筛选/分页/状态流转/删除 + ErrorBoundary，验收通过
- ✅ **Phase 2 Unity AR 端**（`unity/ParkInspectionAR`）：
  - AR Foundation 5.1.5 + ARCore 5.1.5，SceneBuilder 一键搭场景 + 运行时脚本（含状态栏/平面可视化）
  - Android APK 构建成功，真机（iQOO Neo 9 Pro + ARCore）闭环走通
- ✅ **加分项已实现**：GPS `location{lat,lng,accuracy}` 跨设备定位 + 现场照片 `photo` 上报（三端打通）

### 环境备忘
- Unity 2022.3.62f2 中国区版 + Android 工具链（SDK / JDK11 / NDK r23b）已配置
- Go 1.27.0 / Node 24 / npmmirror
- 工程真身在 `D:\UnityProjects\ParkInspectionAR`（ASCII 路径，规避 Unity 非 ASCII 路径报错），工作区 junction 链接

### 已修复的真实 bug（见 `.dshwolf/buglog.json`）
- Unity JsonUtility 序列化 null 字段 `geo` 输出默认结构，正则剔除后残留双逗号 `},,"reporter"` 造成非法 JSON → 已修复 + 回归断言
- PowerShell 5.1 执行含中文 .ps1 报 ParserError → .ps1 需保存为 UTF-8 with BOM
- PowerShell 5.1 Invoke-RestMethod POST 中文 body 落库乱码 → 需 `[System.Text.Encoding]` 显式编码
- Unity Android 构建 JDK/SDK/NDK 连环报错 → Unity 2022.3 要求 JDK 11
- Unity Android 构建非 ASCII 工程路径报错 → 工程真身放 ASCII 路径
